% use utf8;
% my $_id = 'visit';
% layout 'notlogin', page_id => $_id;
% title meta_text($_id);

% if ( $type eq 'visit-info' ) {
  %= include 'visit-info-box'
% }
% else {
  %= include 'visit-box'
% }
